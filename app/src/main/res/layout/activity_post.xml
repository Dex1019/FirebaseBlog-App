<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.dex.firebaseblog.PostActivity">

    <ImageButton
        android:id="@+id/iv_imageSelect"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:adjustViewBounds="true"
        android:background="@android:color/darker_gray"
        android:scaleType="centerCrop" />

    <EditText
        android:id="@+id/et_postTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/iv_imageSelect"
        android:layout_margin="10dp"
        android:background="@drawable/input_outline"
        android:hint="Post Title"
        android:inputType="text"
        android:padding="10dp" />

    <EditText
        android:id="@+id/et_postDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_postTitle"
        android:layout_margin="10dp"
        android:background="@drawable/input_outline"
        android:hint="Post Descripton"
        android:inputType="text"
        android:padding="10dp" />


    <Button
        android:id="@+id/btn_submitPost"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimary"
        android:backgroundTint="@android:color/holo_orange_dark"
        android:text="Submit Post"
        android:textSize="18sp" />

</RelativeLayout>
